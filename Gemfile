# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in mobility_active_storage.gemspec
gemspec

gem "irb"
gem "rake", "~> 13.0"

gem "minitest", "~> 5.16"

gem "rubocop", "~> 1.21"

# Test harness: a minimal Rails application is booted in test/test_helper.rb so
# Active Storage's engine (models, services, jobs) loads the way it does in a real app.
gem "railties", ">= 7.0"
gem "sqlite3", ">= 1.6"
